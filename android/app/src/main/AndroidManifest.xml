<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- Launcher = TrustScore dashboard (root of the 6-section stack);
             consent gate shows automatically on first launch. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-launch purpose-bound consent gate (shown before scoring). -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- The six web sections, mirrored behind the bottom navigation. -->
        <activity
            android:name=".LeaderboardActivity"
            android:exported="false" />
        <activity
            android:name=".SimulatorActivity"
            android:exported="false" />
        <activity
            android:name=".CompareActivity"
            android:exported="false" />
        <activity
            android:name=".FairnessActivity"
            android:exported="false" />
        <activity
            android:name=".ConsentActivity"
            android:exported="false" />
    </application>

</manifest>
